# 기본적으로 main의 yml과 test의 yml은 각각 설정하는것이 좋음
spring:
# 따로 설정 안하면 spring boot가 memory 모드로 실행시켜준다
#  datasource:
#    url: jdbc:h2:mem:test
#    username: sa
#    password:
#    driver-class-name: org.h2.Driver
#  jpa:
#    hibernate:
#      ddl-auto: create-drop
#    properties:
#      hibernate:
#      show_sql: true
#        format_sql: true

logging.level:
  org.hibernate.SQL: debug
  org.hibernate.type: trace